<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %> 
<body>
    <div class="try-page">
    <article>
    <% for(let i = 0; i < restaurants.length; i++)  { %>
        <img src="<%= restaurants[i].image %>">
        <a href="/restaurants/try/<%= restaurants[i]._id %> ">
        <%= restaurants[i].name %> 
        </a> 
    
<% } %> 
    </article>
    </div>
</body>
</html>